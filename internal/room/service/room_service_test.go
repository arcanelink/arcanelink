package service

import (
	"testing"
)

func TestRoomService(t *testing.T) {
	t.Run("package compiles", func(t *testing.T) {
		// Basic compilation test
		t.Log("Room service package compiled successfully")
	})
}
